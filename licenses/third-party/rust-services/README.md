# Rust Service Crate Notices

`rust-services-crates.txt` is generated by `npm run build:third-party-licenses`
before release image builds. It covers the redis-proxy, scheduler, and workflows
service binaries and is intentionally not committed.
