# Supervisor Crate Notices

`supervisor-crates.txt` is generated by `npm run build:third-party-licenses`
before release image builds. It covers the d1-supervisor and do-supervisor
binaries in the workerd runtime image and is intentionally not committed.
